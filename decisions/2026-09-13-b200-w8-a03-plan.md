# B200 width8 A03: one bounded requalification after durable monitor proof

A01 and A02 both reproduced the full2,290-observation numerical anchor but
remain immutable **partial** executions because their supervisors reported
finite cgroup accounting unavailable near shutdown. They jointly cost16 forwards, six VJPs, zero
updates and115.78652374446392 supervisor model-window seconds. They cannot be
silently promoted or overwritten.

The cause-directed monitor change is now independently reviewed and CPU-tested.
It binds fresh allocation memory accounting to the verified initial worker
scope, retains all finite ancestor caps, and allows the v1 exit transition only
for the same process identity. It never substitutes node-wide memory, cached
headroom, a poll grace period, or a relaxed stale/missing telemetry policy. An
allocated A6000 natural-exit qualification, job19817220, passed235 tests,
a120-second GPU child and three CPU exits; it observed the relevant exit
transition with561 samples, maximum gap0.2471 seconds and no stop. That is
execution-plumbing evidence, not a B200 scientific result. The precise
root-membership transition was inferred for the historical B200 archives; it
was directly observed in this new A6000 qualification only.
Its retained local evidence archive SHA256 is
`ba36f9225345267206ea14002eb4c8fde37b460a8ca6b58860ee96ed487fda6c`, and
its summary marker SHA256 is
`b31451aa31601d9c25c311924f725ceddfd1f53e54582735ea6def1b1d4846ec`.

A03 is the one explicit B200 confirmation needed to establish whether the
reviewed mechanism works for the whole-data B200 replay. It keeps the exact
width8 anchor, all2290 rows, source/metric/bundle identities, float64,
determinism, no TF32, full numerical and directional gates, zero optimizer
updates and the existing memory thresholds. It uses300 seconds model work and
1800 seconds saved-only audit/cleanup, inside a40-minute Slurm request. The
shorter model bound is conservative relative to the two measured58-second
application/audit paths, but does not describe a guaranteed runtime or erase
their prior costs.

UVA resource review permits a temporary project cap of four allocated GPUs for
this single B200 cell alongside the three independently queued A6000 P1 cells:
one B200, two CPUs,16GiB, public `gpu`, `gpu:b200:1`, constraint`b200`,
`spinquest_standard`, no requeue and export NIL. It returns to the normal
three-GPU study cap when this cell reaches a terminal state. B200 billing is
substantially higher than A6000 for this profile; measured time-to-result,
queue and cost must be reported before treating it as throughput-improving.
The available allocation balance was not verified, so this is a bounded
requalification, not a statement about broad B200 affordability.

Before claim, use a clean source revision containing the reviewed monitor,
pass the full CPU suite, verify the input bundle and qualification marker in
the allocated environment, and retain the original launch/claim/source/SIF
provenance. Independently review this exact specification and resource request.

## Decision rule

Only a completed worker, passed all-row replay/directional/endpoint audit,
recorded raw endpoint gradient, no supervisor stop, valid fresh resource samples
and terminal scheduler success qualifies B200 execution. Its result must still
be published with all timing, call and resource evidence. A clean A03 permits
separately preregistered B200 width16 then width24 PORT cells, one at a time;
it does not migrate the active A6000 continuations or select an architecture.

Any A03 numerical or execution failure stops B200 promotion and automatic
retries. Preserve the result and return to the qualified A6000 path while a
new diagnosis and explicit human/review decision are considered. This plan does
not authorize a fourth attempt, B200 optimization, or use of A03 hardware time
as evidence of an architecture advantage.
