# B200 width8 A02: repeated numerical pass, execution still partial

The [immutable A02 result](../results/replay-w8-b200-a02/replay-w8-b200-a02-b113439ba956.json)
retains trial/source08b1ae4, claimdb795ef313e5dc16099603ba5b2074bed036b483,
job19815649 and run`replay-w8-b200-a02-b113439ba956`. The job ended with
wrapper exit2; the model worker exited0 and its full-data replay and saved
endpoint audit passed. The correct overall result remains **partial**, not a
clean execution qualification. No previous record or asset is replaced.

All2,290 predictions and fixed directional gates passed again, and the endpoint
SHA256 is exactly the same as A01:
`336c4baf4dba1b06e5b806ea291984209255b0bb6bda026daafd0f9796ff1cf9`.
Eight forwards, three VJPs and zero optimizer updates were charged. This is
reproduction of the fixed anchor, not an improved fitted score.

The supervisor again recorded `finite cgroup limit unavailable` during shutdown.
The first monitor fix only distinguished an exception after the owned child was
already waitable; it did not cover A02. Its terminal-after-owned-exit field is
null. A completed worker summary precedes resource cleanup and does not prove
that the OS process has exited. Saved evidence therefore does not justify
retroactively calling this a passed execution.

Observed setup was36.65s, maximum model call4.22s, supervisor model window
57.88729783613235s and application plus saved audit58.40482343081385s.
Scheduler elapsed was98s after72s in queue. The261 saved resource samples
reported peak owned GPU9.46875GiB, worker-tree RSS1.64085GiB and minimum
allocation headroom14.20291GiB; these are sampled values, not continuous peaks.
Both attempts together cost16 forwards, six VJPs and115.78652374446392s of
supervisor model-window work, plus their separately reported validation, queue
and audit costs. These timings are not a controlled A6000/B200 speedup estimate.

## Decision

Stop automatic B200 retries and width16/24 promotion. Diagnose the lifecycle
gap and require an independently reviewed deterministic regression and fresh
allocation-memory measurements before reconsidering hardware qualification.
The working explanation is a pre-reap worker cgroup-path transition, but A02
did not log the failed sample's process flags or cgroup path; the precise kernel
event remains an inference. Do not weaken missing-memory or one-second-stale
telemetry gates, or substitute node-wide free memory for allocation headroom.

The optimizer study's next [three exact A6000 width trials](2026-09-13-p1-milestone96-plan.md)
are independently reviewed. Briefly hold their claims for the same monitor
lifecycle review so a known potentially shared exit failure is not ignored.
Their numerical contract, saved states, unit policy, call/time ceilings and
three-GPU concurrency plan remain unchanged. Any future B200 technical attempt
needs a new explicit decision/spec/claim; this result authorizes no retry chain.

Independent saved-artifact reviewer: `p1-saved-evidence-review-20260913`.
The result links an independently inspected, uploaded and downloaded-hash-verified
archive with exact source, launchers, claim, inputs/SIF identities, numerical
outputs, telemetry, terminal scheduler accounting and logs.
