# B200 width24 A01: one bounded final PORT execution gate

Completed B200 width8 and width16 PORT results permit exactly one separately
reviewed width24 execution check. This plan fixes the original `p0-w24-064`
all2,290-observation anchor, Nested-FiLM depth1/width24, float64 fixed
physics/operators, no TF32, the saved-reference and directional checks, raw
endpoint-gradient computation, and zero optimizer updates. It binds the CPU
and A6000 references `replay-w24-cpu-a02-fa5d2af0bd21` and
`replay-w24-gpu-a02-c511ed48adc1`.

The original bundle anchor lacks a raw gradient, so the in-worker anchor
preflight cannot compare that array. However, the retained CPU and A6000 width24
PORT endpoints at exactly the same theta do contain full raw-gradient arrays.
After replay, `scripts/compare_saved_raw_gradients.py` must hash-check those endpoint arrays and
require exact theta equality plus maximum absolute raw-gradient error <=1e-7
against each reference. This introduces no model call or optimizer work. The
run archive must retain the saved-only comparison report and both exact
hash-checked reference endpoint files so this new comparison is reproducible
without relying on a publication-pending historical PORT archive. The all-row
prediction/q and directional gates remain unchanged. This is the first
B200 width24 cell, not an automated retry or extension. It preserves the prior B200 execution lineage: width8
qualification24 forwards/nine VJPs/zero updates/173.31992650777102 model
seconds and completed width16 eight/three/zero/58.10458451602608 seconds.

Fresh UVA review permits one W24-only temporary fourth project GPU alongside
the three independently queued A6000 P1 cells: public `gpu`, `gpu:b200:1`,
constraint `b200`, one B200, two CPUs,16GiB, `spinquest_standard`, export NIL
and no requeue. The cell has300 seconds of model work and1800 seconds
saved-only QA/cleanup within a40-minute Slurm request. The exact width24 A6000
replay took147.422 model seconds; this is a2.03x bound without asserting a B200
speedup, queue benefit or cost benefit. The normal three-GPU cap resumes when
this cell becomes terminal.

Before claim or submission, independently review the exact specification and
resource request, pass the complete CPU suite on the exact source, verify the
input bundle/qualification evidence in the allocation, and preserve a distinct
atomic claim and immutable checkout. Passing requires worker/wrapper/scheduler
success, all numerical and directional gates, a saved raw gradient and the
hash-pinned saved-only CPU/A6000 raw-gradient comparison, saved-array audit, no
supervisor stop, actual B200 identity proof and fresh bounded monitor samples.
Publish either terminal outcome.

## Decision rule

This final B200 PORT cell provides execution evidence only. A success ends the
B200 PORT sequence; it does not authorize B200 optimization, change or migrate
the active A6000 P1 trajectories, raise GPU concurrency, establish throughput
or cost superiority, or select an architecture. A failure preserves evidence
and stops automatic B200 promotion/retries. The architecture decision remains
dependent on the separately controlled optimization and paired-seed evidence.
