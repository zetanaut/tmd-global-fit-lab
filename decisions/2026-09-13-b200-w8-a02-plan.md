# B200 width8 A02: bounded execution-qualification retry

The [A01 outcome](2026-09-13-b200-w8-a01-outcome.md) passed all numerical
checks but remains a partial execution. Its immutable record, eight forwards,
three full calls and exact supervisor model-window elapsed time remain part of
the qualification cost. A02 is a new claim and output, not a resumed fit.

Use the same anchor, full2,290 observations, container, numerical tolerances,
float64/no-TF32/determinism and one-B200 resource request. The only execution
change is the independently tested supervisor fix merged in PR13: a sampling
exception is terminal-only when the same owned process has already exited.
Live missing/stale telemetry and the zero-valid-sample gate still fail closed.
All CPU tests run before model execution in the pinned container.

Require completed worker, passed full-data replay and saved audit, valid raw
endpoint gradient and no supervisor stop. A second execution failure requires
diagnosis, not an automatic retry chain. Only a fully passing A02 permits
separately preregistered width16/24 B200 checks. Retain costs of both width8
attempts, and separate queue/setup/model clocks. No architecture is selected.

UVA HPC resource review on13September approves unchanged `spinquest_standard`,
public `gpu`, `gpu:b200:1`, constraint `b200`, one node/task, two CPUs,16GiB and
65minutes; export NIL, no requeue. Maximum one B200 and three total study GPUs
remain active at once. Eligible backfill is observed, but an estimated queue
start is not a guarantee. Independent scientific review is required before claim.
