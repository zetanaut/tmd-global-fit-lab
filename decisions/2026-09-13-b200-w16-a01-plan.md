# B200 width16 A01: one bounded PORT execution gate

The completed width8 B200 A03 result established clean whole-data B200
execution for the reviewed allocation monitor, but it did not qualify another
width. This specification therefore registers exactly one new width16 PORT
cell: the fixed `p0-w16-096` all2,290-observation anchor, float64, fixed
physics/operators, no TF32, the saved-reference and directional checks, raw
endpoint gradient, and zero optimizer updates. The CPU and A6000 reference
runs are `replay-w16-cpu-a02-054110dbf1b0` and
`replay-w16-gpu-a02-45994b868011`. It must retain its own claim, source
revision, input/SIF identities, archive and scheduler evidence.

This is the first B200 width16 attempt, not a technical retry. The three B200
width8 attempts remain separately and immutably accounted: A01/A02 are partial
and A03 passed. Their combined 24 forwards, nine VJPs, zero updates and
173.31992650777102 model-window seconds are recorded as qualification lineage,
not charged to this width16 scientific path and not erased by it.

UVA resource review permits one newly scoped temporary fourth project GPU for
this cell only, alongside the three independently queued A6000 P1 jobs: public
`gpu`, `gpu:b200:1`, constraint `b200`, one B200, two CPUs,16GiB,
`spinquest_standard`, export NIL and no requeue. It returns to the normal
three-GPU cap when the cell is terminal. The bound is300 seconds of model work
and1800 seconds saved-only audit/cleanup within a40-minute Slurm request.
The width16 A6000 replay used176.569843 seconds in its model window, so300
seconds is a1.70x bound without presuming B200 speed or cost superiority.

Before claim or submission, independently review the exact specification and
resource request, run the complete CPU suite on the exact source revision, and
verify the input bundle and allocated-environment qualification evidence. The
cell passes only if worker/wrapper/scheduler all succeed, the all-row numerical,
directional, endpoint-gradient and audit gates pass, the supervisor records no
stop, and fresh bounded allocation samples remain valid. Publish the result
whether pass or fail. Any failure preserves evidence and stops B200 promotion.

## Decision rule

A completed clean width16 B200 execution result permits a separately
preregistered and reviewed width24 B200 PORT cell. It does not permit B200
optimization, migration of the active A6000 P1 trajectories, extra concurrent
B200 cells, or an architecture conclusion. No width24 work, automatic retry,
or resource expansion is authorized by this plan.
